# Configuration globale de construction
[build]
  base = "client" 
  command = "npm install && npm run build" 
  
  # CORRECTION : Le dossier 'dist' est DANS le dossier 'client'
  publish = "dist" 

# Définition des redirections 
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200